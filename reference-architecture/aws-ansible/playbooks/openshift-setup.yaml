---
- name: Running OCP installation playbook
  hosts: localhost
  connection: local
  become: no
  gather_facts: no
  tasks:
  - name: Include variables
    include_vars: vars/aws_openshift.yaml

  - include: /usr/share/ansible/openshift-ansible/playbooks/byo/config.yml
